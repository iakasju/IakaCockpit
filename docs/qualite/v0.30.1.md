# Doc qualité — v0.30.1 (2026-07-14)

> **Correctifs de l'audit v0.29.0** (Legolas) : 1 point sécurité (🟠 M1) + 3 mineurs actionnables.
> Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`1bca334` + `68164fd` + `ace6220` + `922f2e9`) : PASS — `cargo fmt --check` clean,
  clippy 0, **cargo test 337/0**, **vitest 700/0**, typecheck/lint OK. **Faille M1 confirmée fermée.**

## Contexte
L'audit complet (v0.29.0, doc conceptuellement dans le rapport Legolas) a rendu **🔴 0 · 🟠 1 ·
🟡 6**, code globalement sain. Ce patch applique les 4 correctifs actionnables ; m4/m5/m6
(by-design / débranché-gardé) laissés intacts.

## Contenu livré
- **M1 (🟠 sécurité) `1bca334`** — `validate_cwd` (`terminal.rs`) rejetait mal les `..` :
  `<chapeau>/../../etc/passwd` passait le confinement. **Fix** : rejet explicite de tout composant
  `Component::ParentDir` (après `is_absolute`, avant `starts_with`). Test `cwd_avec_parent_dir_est_rejete`
  (`../../etc/passwd` + `sub/../../../etc` rejetés). Les 2 sites (`pty_open` + `pty_runner_open`)
  partagent la fonction → couverts. **Faille fermée.**
- **m2 (🟡 i18n) `68164fd`** — `HandoffReception.tsx` : toutes les chaînes FR en dur externalisées
  via `t()` (namespace `handoff.*`, 21 sous-clés en **parité fr/en**). Restaure la parité EN.
- **m1 (🟡 deps) `ace6220`** — `dompurify` (jamais importé, aucun rendu HTML brut) retiré de
  `package.json` + lockfile. Réduit la surface supply-chain.
- **m3 (🟡 robustesse) `922f2e9`** — locks d'état PTY (`terminal`/`codex`/`transcript`/`voice`)
  passés en **poison-tolérant** (`unwrap_or_else(|p| p.into_inner())`, patron de l'index Analytics) :
  un panic tenant le lock ne condamne plus la session. Comportement inchangé sur le chemin `Ok`.

## Gardes tenues
Façade D7/D8/CSP/`DEMO_DATA_ENABLED=false` inchangés ; non-régression (L22-P3 + Analytics + PTY/chat
intacts) ; aucune nouvelle surface de sécurité (M1 en réduit une).

## Commits (scellés dans v0.30.1)
`1bca334` (M1 `..`) · `68164fd` (m2 i18n) · `ace6220` (m1 dompurify) · `922f2e9` (m3 locks).

## Différés d'audit (non traités, assumés)
m4 (`scan_portfolio`/`add_project` FS non borné, by-design) · m5 (`git.rs::run` dead_code) ·
m6 (`AggIndex.built_at` mort).
