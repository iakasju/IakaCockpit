# Doc qualité — v0.22.0 (2026-07-13)

> **L27 — Filtres de canaux au-dessus du chat** (1er chantier IHM). Front seul.
> Cadré + coordonné par 🟠 Aragorn ; implémenté par 🔴 Gimli ; gate 🟢 Legolas.

## Verdict qualité
- **Gate L27** : PASS — `typecheck`/`lint` OK, **vitest 575/575** (64 fichiers), **aucun `src-tauri/`
  touché** (Rust intact).
- Gardes vérifiées : non-régression de la persistance `pensée`, filtrage préservant les messages
  utilisateur + l'attribution/scroll, non-régression des consommateurs de `Chat` (mode lecture seule
  L25), façade/présentationnel, parité i18n fr/en.

## Contenu livré (L27)
Une **barre de filtres de canaux** au-dessus du fil de messages du chat (`Chat.tsx`) :
- un **chip par canal présent** dans l'historique — **Parole / Geste / Délégation / Activité /
  Pensée** (ordre stable ; barre masquée si ≤1 canal) ;
- **toggle de visibilité** (chip actif = affiché, atténué = masqué ; `aria-pressed`) ;
- **généralise** le toggle `pensée` existant : la **Pensée** reste sur le réglage **persisté**
  (`hidePensee`/`onToggleHidePensee`), les 4 autres canaux = état **local** (MVP) ;
- les **messages utilisateur restent toujours visibles** ; `firstOfRun`/auto-scroll dérivent de
  l'historique **filtré**.

## Commit (scellé dans v0.22.0)
`b382c89` — feat(chat): barre de filtres de canaux au-dessus du chat (L27).

## Différés / suite
- Persistance des 4 autres canaux (config) — MVP local.
- **Chantier IHM (B)** : **arbre des délégations** coloré par avancement (remplace le Gantt), en
  Travail + Journal — lot **L28**, à cadrer/implémenter ensuite.
