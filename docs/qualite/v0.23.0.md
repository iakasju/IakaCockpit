# Doc qualité — v0.23.0 (2026-07-13)

> **L28 — Arbre des délégations** (2ᵉ chantier IHM ; remplace le Gantt). Front seul.
> Cadré + coordonné par 🟠 Aragorn ; implémenté par 🔴 Gimli ; gate 🟢 Legolas.
> Clôt les **deux chantiers IHM** (L27 filtres canaux + L28 arbre délégations).

## Verdict qualité
- **Gate L28** : PASS — `typecheck`/`lint` OK, **vitest 589/589** (66 fichiers), **aucun `src-tauri/`
  touché** (Rust intact).
- Gardes vérifiées : **zéro fausse donnée** (statut `done` best-effort honnête au Journal), **Gantt
  débranché-gardé** (`GANTT_ENABLED = false`, code conservé), `DelegationTree` présentationnel pur,
  parité i18n fr/en, non-régression Travail (onglets L24 / switch L26 / filtres L27) + Journal.

## Contenu livré (L28)
Un **arbre des délégations** coloré par avancement remplace le Gantt :
- **F1 — `DelegationTree`** (pur) : racine = **coordinateur** → **agents délégués** (1 nœud/tâche),
  coloré **`running`** (ambre, pastille pulsée) / **`done`** (vert + ✓), avatars, compteur
  (« N délégations · N en cours »), placeholder si vide. **MVP 1 niveau** (imbrication différée).
- **F2 — Travail** : le bandeau Gantt est **remplacé** par l'arbre (`.treeband`), bouton convhead
  « **Arbre** », alimenté par les délégations de la conversation active (`useAgentTasks`) +
  `activeRunner.coordinator`. **Gantt débranché-gardé** (`GanttPanel`/`derivePlanTimeline`/`timeline`
  conservés, réactivables via `GANTT_ENABLED`).
- **F3 — Journal** : le même arbre pour le projet filtré, via `deriveDelegationsFromFeed` (helper pur,
  canal geste) ; le statut `done` n'est posé **que sur un signal `meta` explicite**, sinon `running`
  (**zéro fausse donnée**). Ancienne liste débranchée-gardée.

## Commit (scellé dans v0.23.0)
`431a538` — feat(delegations): DelegationTree coloré par avancement + remplace le Gantt en Travail,
arbre du projet au Journal (L28).

## Différés tracés
- Statut `done` **fin** au Journal (appariement délégation↔rapport dans le feed L4/L5) — best-effort ici.
- **Sous-délégations imbriquées** (arbre multi-niveaux) — nécessite le lien parent dans `useAgentTasks`.
- Le **Gantt** reste dans le code (réactivable), non affiché.
