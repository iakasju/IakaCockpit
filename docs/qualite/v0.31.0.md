# Doc qualité — v0.31.0 (2026-07-14)

> **L31-P1 — slots multi-runners réels** : lancer un agent de la team comme SON propre runner
> (codex/claude-code) en slot indépendant, multi-onglets par projet. Option C (hybride) tranchée.
> Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`82e003f`) : PASS — typecheck/lint OK, **vitest 724/724** (+12), **front seul**
  (Rust intact). Garde L10 multi-slots + non-régression mono-slot prouvées.

## Contexte (cadrage L31, `specs/instructions/L31-orchestration-multi-runner.md`)
Contrainte dure : le Task/Agent tool de Claude Code spawne des sous-agents **claude** — impossible
de déléguer nativement vers un process Codex/Ollama. Options A (slots), B (re-routage hétérogène,
horizon), C (hybride). **Stéphane a tranché « C + reco partout »** : délégation native claude
conservée + slots multi-runners à la demande ; Ollama/LiteLLM en bannière (MVP) ; slots indépendants ;
lancement depuis le roster ; statut vivant = P2.

## Contenu livré (P1)
- **Modèle de slots** (`useConversations.ts`) : « une conversation = un slot ». Slot d'agent =
  conversation `owned` avec `projectId` **synthétique** (`slotIdFor(realProjectId, agent)`) +
  `slot:{realProjectId,agent,runner,model}`. Coordinateur **inchangé**. `openAgentSlot` idempotent.
  → tailer/tâches/économie/onglets fonctionnent **sans modification** (chaque slot son transcript).
- **UI** : Roster gagne un bouton **« ▶ lancer »** par agent (distinct du `@agent` chat) ; désactivé
  « défini, non câblé » si runner non exécutable (Ollama/LiteLLM). Slots en **onglets** (ProjectTabs).
  Fermer un slot d'agent ferme SON pty+conversation (pas le projet) ; coordinateur = retrait Table L23.
- **Garde L10** : chaque `PtyTerminal` monté une fois, jamais démonté au switch de slot (montés cachés).
  Testé : coordinateur + slot codex → 2 PtyTerminal, rerender → toujours 2.
- **Enforcement par slot** (L22-P3) : `resolveRunner` slot-aware → runner/modèle **de l'agent** +
  `deriveEnforcement(frame, slot.agent)` (allowlist + system-prompt de CET agent).

## Gardes tenues
Garde L10 (multi-slots), non-régression mono-slot, façade D7/D8, CSP, i18n parité fr/en,
`DEMO_DATA_ENABLED=false`, **zéro fausse donnée** (Ollama = bannière, pas de slot dangling).

## Commit (scellé dans v0.31.0)
`82e003f` — feat(orchestration): slots multi-runners réels par projet (L31-P1).

## Différés → L31-P2
- **Statut « vivant » temps réel** par slot (dérivé du tailer) — roster/onglets montrant running/idle.
- **Fermeture en cascade** : au retrait du projet coordinateur, fermer les slots d'agents du même
  projet (aujourd'hui : PTY/tab orphelins, non bloquant — observation gate).
- **Runners « API »** (Ollama/LiteLLM sans TUI) = suite (AR-2). **Re-routage hétérogène (B)** = horizon.
