# Doc qualité — v0.30.0 (2026-07-14)

> **L22-P3 — enforcement runner du Cadre** : le Cadre validé d'une team (`frame.json`) est
> traduit en application réelle sur le runner du coordinateur (`--allowedTools` +
> `--append-system-prompt`). Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`8db6bf8` + `a632c29`) : PASS — `cargo fmt --check` clean, clippy 0, **cargo test
  336/0**, **vitest 700/0** (76 fichiers), typecheck/lint OK.

## Contenu livré
### `8db6bf8` — Dérivation depuis le Cadre (front)
`src/frame/enforcement.ts` (module PUR) :
- `deriveAllowedTools(frame, agentId)` = union des règles **`tool` + `autorisation`** MOINS les
  **`interdit`** (par nom) → liste jointe ; `null` si aucun outil nommé → **repli global**.
- `deriveSystemPromptExtra` = **obligations** + **paragraphes de skills** (texte LLM P2) + **brief**
  de l'agent + **chaîne de délégations autorisée** (texte) ; `""` si rien.
- `deriveEnforcement(frame, coordinatorName)` (match casse-insensible ; agent introuvable →
  repli neutre). + `resolveAgentSkills` dans `model.ts`.
- Câblage : `App.resolveRunner` calcule l'enforcement du frame **de la team du projet** (sinon
  repli global) → `WorkingView`/`PtyTerminal`/`usePty`/`backend.ptyRunnerOpen`.

### `a632c29` — Injection au runner (Rust `terminal.rs`)
`chef_args` gagne `system_prompt_extra`, concaténé **APRÈS l'obligation coordinateur L19**
(jamais à sa place ; `None`/vide → strict historique) ; `resolve_runner_spec`/`pty_runner_open`
propagent ; **codex ignore** l'extra. Précédence allowlist : **Cadre → réglage global → constante**.

## Gardes tenues
- **Repli SANS régression** : Cadre absent/vide → comportement identique à avant (allowlist
  globale + obligation L19 seule). Projets sans frame inchangés.
- Façade D7 (param via backend.ts, camelCase Tauri v2), D8 (dérivation pure/App, pas dans un
  composant), CSP, **zéro fausse donnée** (allowlist/system-prompt du Cadre RÉEL), sécurité
  (ni `--dangerously-skip-permissions` ni `--print`).

## Hors lot / différés
- **Enforcement DUR de la chaîne de délégations** = hook `~/.claude/delegation-guard.mjs` (L5,
  hors dépôt) — non touché ; ici la chaîne autorisée est **exposée en texte** dans le system-prompt.
- `useFrame` porte UN cadre à la fois (portée du sélecteur) : couvrir toutes les teams
  simultanément (chargement du frame par team) = suite possible (MVP : repli global au spawn
  précoce, spawn idempotent).

## Commits (scellés dans v0.30.0)
`8db6bf8` (dérivation Cadre) · `a632c29` (injection runner).
