# Doc qualité — v0.26.0 (2026-07-13)

> **L30 — Page « Analytics »** : nouvelle 6ᵉ vue (remplace Journal dans le rail, Journal
> débranché-gardé) pour comprendre en rétrospective la performance de la configuration
> d'équipe. Livrée en **P1 (enveloppe + 4 perspectives)** puis **P2 (Analytics réel :
> coût $ + délégations réelles)**, avec un lot recette intermédiaire (débranchement des
> data démo). Cadré + coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate P1** (`b6504bc`+`db5fe72`) : PASS — 636 front, Rust intact.
- **Recette P1** (3 correctifs, gates PASS) : `fc1906e` fusion démo par champ · `6d20797`
  plage de temps effective (range-aware + custom) · `ffe3b39` tokens KPI suit la plage +
  composition perAgent range-aware.
- **Débranchement data démo** (`ca4439d`, gate PASS) : `DEMO_DATA_ENABLED=false` — l'app
  recette sur du **réel + placeholders honnêtes**, démo conservée (débrancher-garder).
- **Gate P2** (`a5e9eb7`, branche `feat/L30-P2-analytics-reel`, mergée `3c58918`) : PASS —
  **Rust 300** (fmt/clippy clean) + **front 657**, toutes gardes vérifiées au code.
- **Recette visuelle** : structure/4 perspectives/placeholders honnêtes validés (CDP) ;
  réel (coût $, délégations) confirmé dans la fenêtre Tauri (ex. « 151 délégations »).

## Contenu livré
### P1 — Enveloppe Analytics (front, `b6504bc`…`ffe3b39`)
- Vue `analytics` (rail remplace Journal, **Journal débranché-gardé**) ; colonne **Périmètre**
  (`ALL · portefeuille` + projets tri tokens desc) ; **contrôle de plage** (24h/7j/30j/Custom,
  défaut 7j→now, Custom = 2 dates) **effectif** ; **4 perspectives** (V1 Dashboard, V2 Timeline,
  V3 Comparaison, V4 Par agent) commutables.
- Réel là où dispo (tokens/plage, tokens/jour, coord vs délégués) ; **zéro fausse donnée**
  (placeholder honnête sinon).

### Débranchement démo (`ca4439d`)
- Flag `DEMO_DATA_ENABLED=false` gate les 3 injections de data démo (historique conv,
  seedTasks, widgets démo). Mocks conservés, infra iaka-demo (repo git réel) conservée.
- Délégations = sources **réelles** exclusives (tailer transcript pour Working, feed L4 pour
  Journal).

### P2 — Analytics réel (Rust + front, `a5e9eb7`)
- **Coût $ réel** : `pricing.rs` (table par modèle embarquée bootstrap + **refresh background
  au démarrage** via `pricing_url` config non sensible, offline-first, non bloquant) ;
  `economy.rs analytics_cost(from,to)` calcule le coût par ligne sur les **4 buckets** usage
  séparés (input/output/cache-write/cache-read), agrège `by_model`/`by_day`/`cost_total` +
  `untariffed_models` ; locaux (llama/ollama) = 0 $ ; modèle inconnu = signalé, non compté.
  → remplit KPI coût, tendance, coût cumulé (V2), mix par modèle (V4).
- **Délégations réelles par agent** : `delegations_by_agent(from,to)` apparie `tool_use
  Agent/Task` ↔ `tool_result` (durée) par `subagent_type` → count/total_ms/avg_ms.
  → remplit KPI délégations + classement V4 (comptes + durées).
- Façade `backend.ts` + hooks `usePortfolioCost`/`useDelegationsByAgent` (range-aware, D7) ;
  front ne fait **aucun** fetch réseau (refresh pricing = Rust seul, CSP intacte).

## Réel vs placeholder (honnête)
- **Réel** : coût $ (période/modèle), tendance/cumulé coût, mix modèle, KPI + classement
  délégations par agent (comptes/durées), tokens/plage, tokens/jour, coord vs délégués.
- **Placeholder (pas de source)** : tokens & coût **par agent nommé** (sous-agents hors
  transcript parent, `isSidechain` toujours false), temps agent, top délégations chères ($),
  callout variation, **comparaison V3** bi-période.

## Constat déterminant (gravé)
Sur 66 transcripts réels, `isSidechain` **toujours false** → les tokens des sous-agents
délégués ne sont **pas** dans le transcript parent → attribution tokens/$ **par agent nommé**
sans source → **spike différé** (transcripts séparés des sous-agents).

## Différés tracés
- **Spike per-agent tokens/$** (débloque V3-B hypothèse par agent + « qui coûte quoi » en $).
- **V3 comparaison** bi-période réelle · temps agent · top délégations chères · callout variation.
- **Compaction UI** « zones vides » (masquer les widgets sans source réelle) — lot de suivi.
- Codex non scanné (analytics = `~/.claude/projects`).

## Commits (scellés dans v0.26.0)
`b6504bc` `db5fe72` `fc1906e` `6d20797` `ffe3b39` `ca4439d` · `a5e9eb7` `95e6c1a` (merge `3c58918`).
