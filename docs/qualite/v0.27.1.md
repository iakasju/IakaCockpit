# Doc qualité — v0.27.1 (2026-07-13)

> **Patch recette** : liaisons de l'arbre des délégations rendues visibles. Front seul (CSS).
> Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`ac0226b`) : PASS — typecheck OK, lint OK, **vitest 672/672**, `git show --stat`
  **front seul** (`src/theme/app.css` + test `DelegationTree.test.tsx`), **Rust intact**.

## Contenu livré
Retour terrain Stéphane : « dans l'arbre des délégations, les liaisons ne sont pas visibles ».
Cause : le connecteur `.dtedge` (un « L » isolé par nœud) était trop atténué (`text-3 35%`,
1,5px) et il n'y avait **pas de tronc continu** → la liaison coordinateur→délégués ne se lisait pas.

Correctif (CSS seul, markup inchangé, `DelegationTree` reste pur D8) :
- **Tronc vertical continu** reliant tous les délégués au coordinateur (`.dtkids::before` +
  segments `.dtnode::before` joints).
- **Branche horizontale** ~2px (`.dtedge` repurposé) du tronc vers chaque nœud.
- Contraste **theme-aware** (variable `--dt-line` = `color-mix(text-3 70%)`), lisible clair ET
  sombre (vérifié CDP, `scratchpad/dtree-connectors.png`). Branche colorée par statut
  (running ambre / done vert). Vaut en **Travail ET Journal** (même composant).

## Commit (scellé dans v0.27.1)
`ac0226b` — fix(deleg-tree): liaisons coordinateur→délégués visibles (tronc + branches).

## Différés (Analytics, inchangés)
Comparaison V3 bi-période réelle + hypothèse V3-B (re-tarifage par agent débloqué) · index
incrémental (mtime) · sous-délégations imbriquées (arbre multi-niveaux).
