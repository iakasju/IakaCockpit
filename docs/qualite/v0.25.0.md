# Doc qualité — v0.25.0 (2026-07-13)

> **L29 — révision recette Swimlanes** : labels d'agents fixes + repères d'heure toujours lisibles
> + zoom +/− sur l'axe temps. Front seul. Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (commit `946c1af`) : PASS — `typecheck`/`lint` OK, **vitest 605/605** (67 fichiers),
  **aucun `src-tauri/`** touché (Rust intact).
- Gardes : présentationnel pur D8, zéro fausse donnée conservé, parité i18n fr/en, non-régression
  (toggle Arbre/Couloirs, `DelegationTree`/`GanttPanel` intacts).

## Contenu livré (révision L29, retour terrain Stéphane)
- **R1 — Labels d'agents fixes** : `AgentSwimlanes` scindé en `.swimbody` = colonne `.swimlabels`
  (avatar + nom, **hors** du conteneur `overflow-x`, gelée au scroll) + scène scrollable `.swimscene`
  (axe/gridlines/barres/flèches). Alignement Y par géométrie partagée.
- **R2 — Repères d'heure toujours lisibles** : gridlines verticales + labels `HH:MM` par tick,
  densité dérivée du pixel réel (paliers ronds), se resserre au zoom → aucune portion sans repère.
- **R3 — Zoom axe temps** : boutons `+`/`−` (`useState(zoom)`, bornes `[0.25…4]`, `disabled` aux
  bornes) ; `pxPerMin = 9*zoom` recalcule largeur/barres/flèches/densité des ticks.

## Commit (scellé dans v0.25.0)
`946c1af` — feat(swimlanes): labels agents fixes + reperes horaires + zoom +/- (L29 recette).

## Différés tracés
Passe a11y (colonne labels `aria-hidden`, noms visibles mais non annoncés par lecteur d'écran) ·
imbrication multi-niveaux · prévisionnel/baseline · swimlanes au Journal.
