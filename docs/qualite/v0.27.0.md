# Doc qualité — v0.27.0 (2026-07-13)

> **Analytics réel + recette terrain complète** au-dessus de v0.26.0 (page Analytics P1+P2).
> Rend la page pleinement utile sur **données réelles** : coût $ par période/modèle/projet,
> attribution **par agent nommé** (délégués + coordinateur), et **KPIs précalculés** (instantané).
> Cadré + coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas. 9 commits, gates PASS.

## Verdict qualité
- **Gate final** (`c279ec4`) : PASS — `cargo fmt --check` clean, `clippy -D warnings` 0,
  **cargo test 320/0**, **vitest 670/0**, typecheck/lint OK.
- Gates intermédiaires PASS : paquet `684b1dd`/`31bf696`/`6f4f58c`/`5fcaed7` (attribution) ;
  `a86bb80`/`d11ad62`/`0471294`/`88c9f46` (précalcul + V2 + coordinateur). Un FAIL cosmétique
  `cargo fmt` (drift test) corrigé (`0471294`) puis re-gate PASS.

## Contenu livré (recette terrain, dans l'ordre)
1. **Compaction des zones vides** (`684b1dd`) : les KPIs/widgets sans source réelle ne sont
   plus des skeletons — omis / regroupés en `SoonStrip` ; perspective vide → `EmptyPerspective`.
2. **Réactivité coût par plage** (`31bf696`, test seul) : preuve que `analytics_cost` diffère
   strictement 24h<7j<30j (le « ne bouge pas » initial était transitoire runtime).
3. **Scope par projet** (`6f4f58c`) : coût + délégations suivent le Périmètre (`project` passé
   au Rust `analytics_cost`/`delegations_by_agent`, filtre `project_of(cwd)`).
4. **Attribution par agent RÉELLE** (`5fcaed7`) : tokens + coût **par agent nommé** via le lien
   `toolUseResult.outputFile` (le sous-agent tourne hors du transcript parent, `isSidechain`
   toujours false). Somme des 4 buckets `usage` du sous-agent × prix(`resolvedModel` normalisé
   `[1m]`). Délégations dont l'`outputFile` (tmp éphémère) a expiré → `unavailable` compté,
   **jamais fabriqué**. Remplit V4 + treemap/part V1.
5. **Précalcul des KPIs** (`a86bb80`) : `AggIndex` process-global (RwLock) construit **une fois**
   (background au démarrage + paresseux + `analytics_refresh`) — agrège par (projet,jour,modèle)
   et (projet,jour,agent) ; les commandes **somment l'index** au lieu de re-scanner → **instantané**.
   Le **coût $ reste calculé à la requête** (tokens×prix) → un refresh de prix ne force pas de
   re-scan. Tests `index_*_egal_au_scan_direct` (5 commandes) = non-régression.
6. **Fix V2** (`d11ad62`) : `portfolio_activity` ne tronque plus au top-12 → l'évolution suit
   le projet sélectionné.
7. **Coordinateur en premier rang** (`88c9f46` puis `c279ec4`) : le coordinateur (boucle
   principale = transcript parent, jamais délégué) devient une **ligne d'agent**. Attribué
   **PAR PROJET** au vrai coordinateur (via `AnalyticsCost.by_project` + resolver App/teams) puis
   **agrégé par nom** → en scope ALL, **Aragorn** (somme de ses projets) et **Odin** (portefeuille
   racine, projet non lié) sont **distincts, plus fusionnés**. Tag « coordinateur ».

## Réel vs placeholder (honnête)
- **Réel** : coût $ (période/modèle/projet), tendance/cumulé, mix modèle, tokens/plage,
  tokens/jour (scopé), délégations par agent (comptes/durées), **tokens+coût par agent nommé**
  (délégués via outputFile + **coordinateur** via parent), coordinateur par projet.
- **Placeholder / à venir** : comparaison V3 bi-période, hypothèse V3-B, top délégations $
  historiques dont l'outputFile a expiré (comptées `unavailable`).

## Gardes tenues
Façade unique D7 (aucun invoke hors backend.ts ; front ne lit aucun transcript/outputFile —
100 % Rust), présentationnel D8 (logique teams dans App), CSP intacte (pricing refresh = Rust
`ureq`), i18n parité fr/en, **débrancher-garder** (`DEMO_DATA_ENABLED=false`, mocks conservés),
**zéro fausse donnée** (untariffé → null, unavailable compté, coordinateur seulement si coût réel).

## Différés tracés
- Comparaison V3 bi-période réelle + hypothèse V3-B (re-tarifage par agent maintenant possible).
- Index **incrémental** (mtime) au lieu du rebuild complet.
- Codex non scanné (analytics = `~/.claude/projects`). Table de prix = bootstrap (pricing.json
  autoritaire via refresh). Granularité période = jour.

## Commits (scellés dans v0.27.0, au-dessus de v0.26.0)
`684b1dd` `31bf696` `6f4f58c` `5fcaed7` `a86bb80` `d11ad62` `0471294` `88c9f46` `c279ec4`.
