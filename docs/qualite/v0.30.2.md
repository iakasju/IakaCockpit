# Doc qualité — v0.30.2 (2026-07-14)

> **Lot polish** (4 petites améliorations UI, front seul) : persistance de toggles, a11y
> (treemap clavier + aria-label webhook), bouton « + » onglets. Cadré+coordonné 🟠 Aragorn ·
> impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`c9dcde7` + `8f1c7a3` + `63f5dd7` + `89db738`) : PASS — typecheck/lint OK,
  **vitest 712/712** (77 fichiers), front seul (Rust intact).

## Contenu livré
- **P1 `c9dcde7`** — Persistance des toggles : hook réutilisable `usePersistentState` (localStorage,
  SSR/test-safe, corruption → défaut silencieux) appliqué à `shelfView` (Liste/Tuiles de l'Atelier)
  et `delegView` (Arbre/Couloirs). Ne persiste que des **toggles cosmétiques** (la config métier
  reste `useSettings`/Rust — façade D7 non détournée).
- **P2 `8f1c7a3`** — a11y : `aria-label`+`title` i18n (parité fr/en) sur le bouton « Tester l'envoi »
  (Réglages, canal adresse n8n).
- **P3 `63f5dd7`** — a11y : treemap Économie accessible au clavier (`role="button"`/`tabIndex=0`/
  `aria-label`, Enter/Espace = double-clic ; cellule inerte sans `onOpenInWork`). Souris inchangée.
- **P4 `89db738`** — bouton « + » dans `ProjectTabs` (frère de `.projtabs-list`, avant `.fsswitch` →
  layout 2-lignes v0.27.3 + collage droite v0.21.0 non régressés) ; câblé sur le flux existant
  `onAddProject` (import + workset), sans nouveau picker ; `aria-label` i18n.

## Gardes tenues
Façade D7 (localStorage réservé aux toggles cosmétiques), D8, CSP, i18n parité fr/en, garde L10
(PtyTerminal jamais démonté) non affectée, `DEMO_DATA_ENABLED=false`, non-régression (712 verts).

## Commits (scellés dans v0.30.2)
`c9dcde7` (persistance) · `8f1c7a3` (aria webhook) · `63f5dd7` (treemap clavier) · `89db738` (bouton +).
