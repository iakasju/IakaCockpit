# Doc qualité — v0.27.2 (2026-07-13)

> **Patch recette perf** : build de l'index Analytics en **2 phases** → Périmètre + KPIs
> tokens/coût **quasi immédiats** à l'ouverture (l'attribution par agent, plus lente, se
> résout ensuite), + indicateur de construction. Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli ·
> gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`f627938` + `ab8e70b`) : PASS — `cargo fmt --check` clean, clippy 0, **cargo test
  321/0**, **vitest 675/0**, typecheck/lint OK. Non-régression **index==scan direct** intacte.

## Contexte (retour terrain Stéphane)
« Je ne vois pas iakacockpit dans les projets » (Périmètre Analytics). Cause : le Périmètre
(`portfolio_economy` → `query_index`) attendait le build **complet** de l'index — y compris la
lecture des ~204 `outputFile` d'attribution (plusieurs secondes) — alors qu'il n'a besoin que
des tokens. Sans indicateur, le Périmètre paraissait vide au démarrage (IakaCockpit est pourtant
le projet n°1, 12 932 lignes). *(À part : le travail iakacockpit est fragmenté en `IakaCockpit`/
`src-tauri`/`docker`/`redesign`… — repli sur la racine git = décision séparée, non traitée ici.)*

## Contenu livré
### `f627938` — build en 2 phases (Rust)
- `AggIndex` : `tokens_built` / `attrib_built` + `pending`.
- **Phase 1 (rapide, parsing pur, AUCUN `outputFile`)** : `tokens` + `deleg` → sert
  `portfolio_economy`/`portfolio_activity`/`analytics_cost`/`delegations_by_agent`, construite
  **synchrone au 1ᵉʳ accès** → Périmètre servable en **sous-seconde**.
- **Phase 2 (lente, I/O hors verrou)** : `resolve_pending` lit chaque `outputFile` une fois →
  `attrib`/`unavailable` → sert `agent_attribution` (non bloquante, trigger en fond
  anti-double-spawn CAS). `spawn_build` = phase 1 puis 2. Commande `analytics_index_status`.
- Non-régression : résultats FINAUX identiques (helper phase1+`resolve_pending`) ; 5 tests
  `index_*_egal_au_scan_direct` verts + `phase1_sert_le_perimetre_sans_lire_les_outputfiles`.
  Pas de deadlock dans `query_index` (read-guard temporaire droppé avant le write).

### `ab8e70b` — indicateur de construction (front)
- `analyticsIndexStatus()` + `useIndexStatus` (poll **borné**, stop à `attrib_ready`, plafond 40,
  hors-Tauri → prêt immédiat). Bannière discrète « Construction de l'index… » tant que phase 1
  pas prête ; **re-fetch** de l'attribution quand `attrib_ready` bascule. V4 « Calcul par agent
  en cours… » (au lieu de « pas de source ») jusqu'à la phase 2. i18n fr/en, CSS `.ana-building`.

## Gardes tenues
Zéro fausse donnée (V4 « en cours » ≠ donnée inventée ; untariffé→null ; unavailable compté),
façade D7 (poll borné, aucun invoke hors backend.ts), D8, CSP (front ne lit aucun `outputFile`),
i18n parité, `DEMO_DATA_ENABLED=false`.

## Commits (scellés dans v0.27.2)
`f627938` (perf 2-phases) · `ab8e70b` (indicateur).

## Différés
- **Repli des sous-dossiers sur la racine git** (fragmentation `src-tauri`/`docker` → `IakaCockpit`)
  — décision produit en attente de Stéphane.
- Index **incrémental** (mtime) · comparaison V3 bi-période réelle + hypothèse V3-B · arbre multi-niveaux.
