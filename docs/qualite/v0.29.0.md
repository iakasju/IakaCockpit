# Doc qualité — v0.29.0 (2026-07-13)

> **Trois différés Analytics** (index incrémental · arbre de délégations multi-niveaux ·
> hypothèse V3-B par agent). Clôt le polish de la page Analytics.
> Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate groupé** (`15c5e6a` + `c8d0601` + `047dd90`) : PASS — `cargo fmt --check` clean,
  clippy 0, **cargo test 332/0**, **vitest 689/0**. Non-régression du refactor index confirmée.

## Contenu livré
### `15c5e6a` — Index INCRÉMENTAL (mtime) (Rust `economy.rs`)
Cache **par fichier** (`path → { mtime, size, fragment tokens/deleg/pending, attrib résolu }`).
Au build/refresh : `stat` chaque `.jsonl`, **re-parse uniquement** les nouveaux/modifiés,
réutilise les fragments inchangés, retire les disparus. Phase 1 **et** phase 2 incrémentales
(outputFiles relus seulement pour les fragments modifiés). Index = **merge des fragments**
(sommes commutatives → identique au parse complet). Défensif (stat KO → re-parse ; lecture KO →
fragment vide). Correctness prouvée (`incremental_egal_au_build_complet_apres_modif`, compteur
re-parse 2→0→1, disparu retiré) ; non-régression `index_*_egal_au_scan_direct` verte.

### `c8d0601` — Arbre de délégations MULTI-NIVEAUX (Rust + front)
`scan_output` extrait, pendant la lecture d'un `outputFile` (phase 2), les `tool_use Agent/Task`
→ **arêtes parent→enfant** (`subagent_type`, statut best-effort, ts ; edges cachées par fragment
→ incrémental). Commande `delegation_edges(from,to,project)`. Front `DelegationTree` **récursif**
(indenté, tronc+branches par niveau, **anti-boucle `seen` + `MAX_DEPTH=4`**), branché au
**Journal** (rétrospectif, `useDelegationEdges`). **MVP profondeur 2-3** ; **live (Travail)
reste 1 niveau — différé**. edges absents → 1 niveau (zéro fausse donnée ; arête seulement si
réellement présente).

### `047dd90` — Hypothèse V3-B ciblée PAR AGENT (front)
`repriceHypothesis(override={agent?,fromModel?,toModel})` : `agent` présent → re-tarife
**uniquement cet agent** (les autres gardent leur coût réel) ; sinon global (conservé). Toggle
« par modèle / par agent ». **Tokens constants** ; **étiquette « hypothèse · à volume constant »
toujours affichée** (maintenue après bascule) ; cible sans prix → null ; untariffé/`unavailable`
exclu.

## Gardes tenues
Façade D7 (`delegation_edges` via backend.ts ; front ne lit aucun transcript/outputFile), D8,
CSP, i18n parité fr/en, `DEMO_DATA_ENABLED=false`, zéro fausse donnée, perf via index.

## Commits (scellés dans v0.29.0)
`15c5e6a` (index incrémental) · `c8d0601` (arbre multi-niveaux) · `047dd90` (hypothèse par agent).

## Différés (Analytics) restants
- Arbre de délégations **multi-niveaux en LIVE** (Travail) — le Journal l'a en rétrospectif.
- Récursion des sous-délégations **au-delà de 2-3 niveaux** (outputFiles des sous-délégations).
