# Doc qualité — v0.17.0 (2026-07-12)

> Jalon IHM Portefeuille + retours terrain. Scellé après **2 gates Legolas PASS**.
> Cadré + coordonné par 🟠 Aragorn ; implémenté par 🔴 Gimli ; gate 🟢 Legolas.

## Verdict qualité

- **Gate 1 (6 commits)** : PASS — 499/499 front (F1/F2), puis relance complète.
- **Gate 2 (5 commits)** : PASS — **538/538 front (62 fichiers)** + **274/274 Rust**,
  `typecheck` / `lint` / `cargo fmt --check` / `cargo clippy -D warnings` verts.
- **Couverture** : 74,9 % stmts (fichiers touchés bien couverts : `urgency.ts` 100 %,
  `ShelfRow` 100 %, `useTeams` 96,5 %, `ProjectCard` 91 %).
- Rust confiné à `portfolio.rs` (un seul commit le touche) ; tout le reste = front pur.
- Gardes tenues : façade unique D7, présentationnel D8, CSP intacte, parité i18n fr/en,
  zéro fausse donnée.

## Contenu livré

### L16 — Page Portefeuille (Étagère)
- **F1** — Toggle **Liste ↔ Tuiles** de la section Atelier (défaut Liste ; table toujours en
  tuiles ; mode Tuiles = cartes `.proj` réutilisées avec action « poser sur la table »).
- **F2** — **Double-clic** sur une cellule du widget Économie (treemap) ⇒ bascule sur Travail
  avec le projet au premier plan (`openProject` + `setActiveView`), sans mutation du workset,
  scoping AR-4 conservé.
- **F3** — Tuile projet **enrichie** : **description en gras** (source **`specs/PROJET.md`
  prioritaire** — AR-6=B, fallback CLAUDE.md `## Ce qu'est ce projet` puis dernier commit) +
  ligne **`next :`** (1er `- [ ]` du backlog) + méta **version / N commits de retard / N
  étapes restantes** (masquée si absente). Étend le scan Rust (`portfolio.rs` :
  `read_description` / `read_backlog`, 3 champs sur `Project`).
- **F4** — **Pastille d'urgence** à gauche du titre, dérivée de `backlog_remaining` : 🔴 ≥ 5,
  🟠 1-4, 🟢 0 (fini), gris si pas de backlog (AR-7, seuil N=5). `read_backlog` distingue
  désormais `Some(0)` (tout coché) de `None` (pas de backlog).
- **F4-bis** — Pastille d'urgence **partagée et identique** liste ↔ tuile (helper
  `src/components/urgency.ts`, source unique du mapping ; `ShelfRow` adopte la pastille, son
  statut git restant dans le `.meta`).

### L23 — incrément
- Au **retrait d'un projet de la Table** (bouton −), **fermeture du PTY (shell/runner) ET de
  la conversation** (`useConversations.closeConversation` + `pty.close`), le job de préparation
  de reprise étant conservé. Garde L10 intacte (fermeture uniquement au retrait explicite).

### Retours terrain / débranchements (règle « débrancher, garder »)
- **Page « Cadre » débranchée du rail** (bouton de nav retiré ; tout le code Cadre conservé).
- **Affichage du statut « préparation de reprise » débranché** (le job continue de tourner).

### Cadre & obligation coordinateur
- Règle typée **`obligation` `oblig-def-projet`** ajoutée au frame par défaut (attachée au
  coordinateur) : « le coordinateur maintient la ligne de définition du projet dans
  `specs/PROJET.md` (création + évolution, validée par le user) ». Inscrite aussi en **mémoire**
  et dans les **contrats de rôle** coordinateur (aragorn/odin, hors dépôt).

### Fix — vignettes des personas
- Bug « Aragorn = Gandalf » : team par défaut **legacy** persistée avec `Aragorn.roleIndex=2`
  (dupliqué). **Réparation one-shot au chargement** : `reconcileDefaultTeamCasting` réaligne
  `roleIndex`/`royaume` des agents canoniques + le coordinateur corrompu de la **seule**
  `DEFAULT_TEAM_ID`, **non destructif** (agents/coordinateur/teams custom intacts), idempotent.
  Garde anti-récidive dans `TeamsEditor` (roleIndex piloté par le menu de rôle + warning
  d'unicité). Audit complet : aucune autre collision.

## Commits (scellés dans v0.17.0)
`601e7ae` F1 · `76219a9` F2 · `de1e6d2` F3 · `87f898a` F3/AR-6=B · `8ac2a44` L23-inc ·
`d1c0de4` débranche Cadre · `d71b87f` F4 · `119dada` F4-bis · `e148c77` débranche statut reprise ·
`937c603` règle Cadre obligation · `9a0777a` fix vignettes roleIndex.

## Différés tracés (non bloquants)
Persistance du mode toggle (`ui_portfolio_atelier_view`) · a11y clavier des cellules treemap ·
confirmation au retrait · suppression du champ numérique `roleIndex` (garde par warning
conservée) · matérialisation de l'obligation coordinateur dans l'**enforcement runner** (Cadre
L22-P3) · L16 vocal (whisper n'entend pas encore la voix, hors ce jalon).
