# Doc qualité — v0.15.0

> Scellée le 2026-06-30. Candidate `v0.15.0`. Précédente : `v0.14.0`.

## Thème de la version

**Refonte Portefeuille/Atelier (L21) + finition de recette terrain.** La branche
`fix/portefeuille-recette-terrain` (10 commits) porte la refonte conforme au mock
(`portefeuille.html`) achevée à la version précédente, **plus la résolution du bug Économie
qui bloquait la pause** et deux ajustements de recette terrain sur les widgets de l'Étagère.

## Lots livrés

- **L21 — Refonte Portefeuille/Atelier** (cartes riches « Posés sur la table » : chip statut,
  description = dernier commit, vignettes superposées, anneau coût % ; section « Rangés dans
  l'atelier » + bouton poser ; scoping anneau/treemap à la table, AR-4 ; visu « Travail
  récent » = scatter-timeline du portefeuille réel entier, AR-5 révisé). Finition Loki :
  treemap mosaïque + segments pilule + légende, ombre hairline dark, 3ᵉ ligne KPI.
- **Fix bug Économie** (`539ea7f`) : le treemap « coût par projet & agent » (scopé table)
  restait **vide**. Deux causes corrigées dans `economy.rs` :
  - `project_of(cwd)` coupe désormais sur `/` **ET** `\` → les clés des anciennes sessions
    Windows (`C:\…`) sont nettoyées en nom de projet (décision Stéphane : **« tout garder »**,
    aucun filtrage portefeuille).
  - `portfolio_economy()` ne tronque plus à top-8 **avant** le scope front : renvoie tous les
    projets, le front scope à la table puis la treemap prend son propre top du sous-ensemble.
    Les petits projets de la table (ex. `iaka-demo`) ne sont plus jetés en amont.
- **Ajustements de recette terrain** (front pur) :
  - **Travail récent** (`fb3a5f7`) : retour aux **polices standard** (annulation du « ×2 »),
    **interligne resserré** (rowH 60→26, invariant anti-collision Ø≈24 ≤ 26 préservé),
    **5 projets visibles + scrollbar** conservés.
  - **Économie** (`ff20df3`) : corps de la treemap **borné (`max-height:390px`) + scrollbar**
    (mosaïque + légende), **aucune donnée tronquée** ; placeholder vide intact.

## Qualité (gate Legolas — PASS)

- **Rust** : 232 tests verts (dont 11 `economy`, nouveau `project_of_gere_les_cwd_windows`),
  `cargo fmt --check` OK, `cargo clippy -D warnings` **0 warning**.
- **Front** : 401 tests verts (49 fichiers), `tsc --noEmit` OK, ESLint **0 warning**.
- **Recette terrain GUI** (Stéphane, `tauri dev` recompilé) : Économie non vide + scrollable,
  Travail récent en polices std + interligne resserré + scroll à 5 → **validé**.

## Différés tracés

- **État démo sur seed one-shot** : `useDemoSeed` n'active les widgets démo que si
  `seedDemo()` renvoie `seeded:true` (faux sur env déjà seedé) → « table vide » au
  relancement. Découpler du seed one-shot si on veut un état démo dev fiable (hors périmètre
  de ce fix).
- **#5b** : treemap de coût **cross-projet** (agrégation multi-transcripts) — Étagère KPI.
- **Capture hors-cockpit** : le dev fait hors iakacockpit est mal couvert (hook env-gated,
  tailer = session active). Idée daemon iakaframe pré-calculant les KPI.
- Suite L16 (pilotage vocal, cadré, non démarré).
