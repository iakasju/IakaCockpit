# Doc qualité — v0.18.0 (2026-07-12)

> Jalon vue Travail : fenêtres de travail toujours ouvertes + onglets par projet.
> Cadré + coordonné par 🟠 Aragorn ; implémenté par 🔴 Gimli ; gates 🟢 Legolas.

## Verdict qualité
- **Gate L24 (2 commits)** : PASS — 549/549 front, Rust non touché.
- **Gate fix en-tête (1 commit)** : PASS — 550/550 front, Rust non touché.
- `typecheck` / `lint` verts. Rust **non modifié** dans tout le jalon.

## Contenu livré

### L24 — Onglets par projet + fenêtres de travail toujours ouvertes
- **F1 — Ouverture eager** : dès qu'un projet **lié** est posé sur la Table, sa fenêtre s'ouvre
  automatiquement (runner lancé). Helper pur `src/app/reconcileEagerOpen.ts`
  (`projectsToEagerOpen` : ouvre ssi `hasBinding && !déjà ouvert`) + effet dans `App` (deps
  contrôlées, `openProject` via ref, convergence anti-boucle, anti-empilement de popups AR-3 :
  seuls les liés auto-ouverts).
- **F2 — Barre d'onglets par projet** : composant présentationnel `src/components/ProjectTabs.tsx`
  (un onglet par conversation ouverte, nom du projet, actif mis en évidence, « × » = retrait de la
  Table via L23-inc). Câblé dans `WorkingView` (`onSelect`=`setActive`, `onClose`=retrait). La
  **worklist gauche est conservée** (coexistence AR-1) et reste synchronisée.
- **F3 — Toggle Shell/Conversation par onglet** : déjà par conversation, préservé. Chaque projet
  garde son mode et son runner **vivant** — **garde L10 intacte** (PtyTerminal jamais démonté au
  switch/toggle ; fermeture uniquement au retrait explicite).

### Fix — en-tête de conversation (recette terrain)
- Le nom du **coordinateur** ne s'affiche plus que s'il **diffère** de l'interlocuteur (fin du
  double « Aragorn » empilé dans la `.convhead`).
- L'en-tête tient sur **une seule ligne** : le chip runner se tronque (ellipsis), le toggle
  Chat/Shell et le bouton « Interrompre (esc) » ne sont plus clipés ni percutés (`convtitle`
  `flex-wrap: nowrap`).

## Commits (scellés dans v0.18.0)
`17e0973` L24-F1 (ouverture eager, helper pur) · `a2215f5` L24-F2 (onglets par projet) ·
`4e2086b` fix en-tête conversation (double coordinateur + collision esc).

## Différés tracés (non bloquants)
Réordonnancement des onglets (DnD) · garde perf N runners simultanés · bouton « + » d'ajout dans
la barre d'onglets · onglet pour projets non liés · **L25** (s'attacher à la session vivante d'un
projet — voir la conversation en cours, cadré, cible v0.19.0).
