# Doc qualité — IakaCockpit v0.12.0

> Généré au scellement de la **version mineure v0.12.0** (règle de méthode : version mineure →
> revue de code complète + revue qualité, puis ce doc une fois le gate PASS).
> Revue conduite par 🏹 Legolas. Verdict : **PASS**. Date : 2026-06-28.

---

## 1. Périmètre de la version (depuis v0.11.0)

- **Refonte UI — direction A** (mock Loki, validé Stéphane) : **rail de navigation en texte**
  (Portefeuille · Travail · Journal · Équipes · Réglages), grammaire **sans-bord / aérée**
  (fin des cartes bordées-ombrées), police **Space Grotesk + Inter bundlées** (`'self'`, CSP
  intacte), **défaut charte = studio-clair**, Réglages débarrassés des emojis décoratifs.
- **Page Teams en liste + fiche** (fin du formulaire vertical) ; interlignage des titres resserré ;
  segments Réglages alignés à droite ; barre de filtre Journal sur une seule rangée.
- **Vignettes WebP 256px** : re-sync depuis iakagraph → **118 Mo de PNG pleine réso remplacés
  par ~9,6 Mo de WebP** embarqués (−92 %), résolveur inchangé, fallback pastille intact.
- **7 rôles canoniques** (`portefeuille · coordination · architecture · fabrication · tests ·
  graphisme · doc`, `src/theme/roles.ts`) ; **team iakaframe par défaut à 7 agents** (un par rôle,
  Odin→Nathalie) ; champ rôle de l'éditeur = **menu fermé**.
- **Internationalisation react-i18next FR/EN** : sélecteur de langue dans Réglages (config
  `ui_lang`), deps **bundlées zéro-distant**, parité fr/en garantie par test ; **rail FR cohérent**.
- **Panneau « Tâches en cours »** (Working, colonne droite) : délégations live dérivées du
  transcript (`delegation`→running / `activite`→done par `tool_use_id`, défensif) + **vitrine démo**
  (4 tâches mockées, démo-only, injectées seulement pour `iaka-demo`).

---

## 2. Résultats qualité (chiffres réels, HEAD = 08c2b84)

| Étape | Résultat |
|---|---|
| `tsc --noEmit` / ESLint | OK — 0 problème |
| vitest (front) | **332 / 332** (35 fichiers) |
| `cargo fmt --check` / `clippy -D warnings` | OK — 0 warning |
| `cargo test` | **217 / 217** (Rust **non touché** par v0.12) |
| `npm run build` | OK (WebP + woff2 bundlés, 0 origine distante) |

**Couverture** : **83.59 % stmts · 75.08 % branches · 76.79 % funcs · 84.86 % lignes** — en
**hausse** vs v0.11.0 (81.81 / 73.08 / 83.55).

---

## 3. Constats de revue & statut

| Sévérité | Constat | Statut |
|----------|---------|--------|
| INFO | `SettingsView.tsx:715` `aria-label="URL du webhook n8n"` non extrait (i18n). | Accessibilité only, invisible, hors parité. Appoint. |
| INFO | Noms de chartes (`CHARTES.name`) mixtes FR/EN (« OS Windows », « Studio clair »…). | Identifiants de marque/casting — à i18n-iser ou assumer. |
| INFO/OPS | Historique git ~118 Mo (anciens PNG vignettes). | **À purger avant le 1er push** (working tree = 9,6 Mo WebP). |
| INFO | Codex : tolérance rollout 2 s, shapes d'outils à confirmer (reporté v0.11). | Recette session réelle. |
| INFO | Couverture UI basse (PtyTerminal/NextStepPanel/App shell). | Par conception (logique dans les hooks). |

**Aucun défaut bloquant.**

---

## 4. Invariants vérifiés

- **Façade unique** : `invoke` hors `src/api/backend.ts` = 0.
- **CSP** stricte jamais null ; police, vignettes WebP, locales i18n servies en `'self'` ;
  **zéro origine distante** (dist vérifié — pas de googleapis/gstatic/CDN i18n).
- **Secrets keychain** write-only ; `is_secret` robuste au namespace `project_team:` (fix V1 tenu).
- **Cœur runner L10/Codex** : `src-tauri/` **non touché** depuis v0.11 → `terminal.rs`/`transcript.rs`/
  `codex.rs` non régressés (217 verts).
- **Garde L10** : le PtyTerminal reste **monté-caché** (`display:none`) ; la colonne droite `.wkright`
  (Roster + TasksPanel) est un `<aside>` frère → le runner survit au toggle Chat/Shell et à l'ajout
  du panneau.
- **`useAgentTasks`** défensif : appariement par `tool_use_id`, events sans/avec id inconnu ignorés,
  `done` jamais rétrogradé, stabilité de référence (pas de re-render parasite) — verrouillé par tests.
- **i18n** : parité fr/en (test), deps bundlées. **Socle L0** : pathguard anti-traversal, paths
  cross-OS. **Pas de god-component**.

---

## 5. Reste en recette manuelle (`tauri dev`)

- Repeinture réelle des 10 chartes + 3 castings (couleurs/rayons exacts, polices web en fallback).
- Bascule FR/EN réelle + **qualité linguistique des traductions EN** (Stéphane/Loki).
- Session **Codex réelle** (trust, TUI xterm, shapes outils, découverte rollout).
- Panneau Tâches en **délégation réelle** (non mockée) sur un vrai transcript de chef-runner.
- Édition teams 7 rôles + popup TeamPicker ; main courante L4 sur CouchDB réel ; doc geste L5 dans Journal.

---

## 6. Différés connus

aria-label webhook à extraire · noms de chartes à i18n-iser (ou assumer marque) · **purge de
l'historique git 118 Mo avant push** · runners RÉELS par agent / orchestration multi-agent · exécution
ollama/litellm (bannerisés) · allowlist/trust par agent (globaux, AR-3) · live-switch coordinateur ·
rendu xterm TUI lent au scroll · idempotence partielle `_id` côté L5 (hors dépôt).

---

## 7. Verdict

**PASS** — version **cohérente, sûre et verte** (332 front + 217 Rust, 0 warning, couverture en
hausse à 83.6 %). Socle L0 et cœur runner L10/Codex **intacts** (Rust non touché), le neuf (refonte
A, vignettes WebP, 7 rôles, i18n bundlé, panneau Tâches défensif, vitrine démo bornée) sain et gaté,
garde L10 tenue, invariants durs respectés (façade=0, CSP `'self'`, is_secret V1). **Candidate
`v0.12.0` scellée.**
