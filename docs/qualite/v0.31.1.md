# Doc qualité — v0.31.1 (2026-07-14)

> **L31-P2** : statut « vivant » par slot (dérivé du tailer) + fermeture en cascade des slots
> orphelins au retrait du projet. Front seul. Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli ·
> gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`ee8f71b` + `053dce2`) : PASS — typecheck/lint OK, **vitest 749/749** (78 fichiers),
  **front seul** (Rust intact). Garde L10 (cascade-close) + honnêteté du statut prouvées.

## Contenu livré
### `ee8f71b` — Statut vivant par slot (AR-5)
- Module pur `src/hooks/useLiveStatus.ts` : `deriveLiveStatus(lastEventTs, now, 20s)` → `running`/
  `idle` (défensif : undefined/futur/expiré → idle) ; `deriveRosterLiveStatus` → par agent
  `running`/`idle`/`none`. Hook `useLiveStatus` marque l'horodatage du dernier event par slot (throttlé).
- Roster : indicateur ● en cours / ○ au repos / — non lancé (coordinateur inclus ; **prime** sur le
  repli L8, sinon repli conservé). ProjectTabs : point de statut discret par slot. i18n fr/en.
- **Limite honnête documentée** : c'est la **fraîcheur du flux d'events du tailer**, PAS un vrai
  signal de process (un runner bloqué en attente d'entrée apparaît « au repos »). Vrai temps réel
  (DEP-1) reste différé. Zéro fausse donnée (pas de slot → none).

### `053dce2` — Fermeture en cascade des slots (fix observation gate P1)
- `removeFromWork` ferme les slots du **même** projet (`slot.realProjectId===projectId`) —
  `closePty` (si exécutable) PUIS `closeConversation` par slot — avant le coordinateur. Fin des
  tab/PTY orphelins.
- **Garde L10** : le PtyTerminal d'un slot se démonte APRÈS la fermeture explicite de SON PTY ;
  aucun slot d'un autre projet touché ; coordinateur non double-fermé.

## Gardes tenues
Garde L10, non-régression (mono-slot, L31-P1, L23/L25, repli L8 Roster), façade D7, D8, CSP,
i18n parité fr/en, `DEMO_DATA_ENABLED=false`, zéro fausse donnée.

## Commits (scellés dans v0.31.1)
`ee8f71b` (statut vivant) · `053dce2` (cascade-close).

## Différés → horizon
- Vrai signal de process « vivant » temps réel (DEP-1) · runners « API » Ollama/LiteLLM (AR-2 suite)
  · re-routage hétérogène (Option B) — tous horizon.
