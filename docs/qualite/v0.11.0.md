# Doc qualité — IakaCockpit v0.11.0

> Généré au scellement de la **version mineure v0.11.0** (règle de méthode : version mineure →
> revue de code complète + revue qualité, puis ce doc une fois le gate PASS).
> Revue conduite par 🏹 Legolas (gate qualité). Verdict final : **PASS**. Date : 2026-06-27.

---

## 1. Périmètre de la version (depuis v0.10.0)

- **L12 — Journal** : la main courante 3-canaux (L4) sort de Portfolio dans une **page dédiée**
  (`JournalView`, 4ᵉ vue) ; Portfolio recentré sur le portefeuille.
- **L13 — Page Teams** : l'éditeur teams/agents/skills (`TeamsEditor`) sort de Réglages dans une
  **vue dédiée** (`TeamsView`, 5ᵉ vue). Nav à 5 vues (`portfolio | working | journal | teams |
  settings`).
- **L14 — Chartes iakagraph** : Réglages → Charte liste et **applique les 10 chartes**
  d'iakagraph (avant : 2 naonedge en dur). `scripts/sync-chartes.sh` synchronise les
  `tokens.css` via un **pont de 23 variables** (contrat app ← contrat iakagraph) → `chartes.css`
  embarqué (`'self'`) + `manifest.ts`. naonedge dark/light gardés hand-written (défaut).
- **Runner Codex** : 2ᵉ **runner réel** (la vision multi-runner devient concrète). `RunnerSpec`
  codex → TUI native `codex` (binaire bundlé `/Applications/Codex.app/…`) en PTY ; `codex.rs`
  tailer du **rollout JSONL** (`~/.codex/sessions/…`) avec mapping codex ; refacto `tail_resolved`
  partagée Claude/Codex (non-régression Claude prouvée au code). Bannière levée pour `codex` ;
  `ollama`/`litellm` restent bannerisés.

*(Hors dépôt Cockpit, gaté séparément le même jour : **L5 — Traçage MACHINE des délégations**
— hook `~/.claude/delegation-guard.mjs` + `iakaboxlogs` ; une délégation émet un doc
`meta.canal:"geste"` en CouchDB, lu par L4/Journal.)*

---

## 2. Résultats qualité (chiffres réels, HEAD = 4ebafba)

| Étape | Résultat |
|---|---|
| `tsc --noEmit` | OK |
| ESLint | OK — 0 problème |
| vitest (front) | **291 / 291** (32 fichiers) |
| `cargo fmt --check` | OK |
| `cargo clippy --all-targets -D warnings` | OK — 0 warning |
| `cargo test` | **217 / 217** |
| `npm run build` | OK |

**Couverture** (`npm run test:coverage`) : **81.81 % stmts · 73.08 % branches · 74.54 % funcs ·
83.55 % lignes** — en **hausse** vs v0.10.0 (77.22 / 65.81 / 78.88). Points bas = composants UI
(`PtyTerminal` 0 % xterm, `NextStepPanel` 0 %, `App` shell 41 %, `MainCourante` 45 %), **par
conception** (logique dans les hooks fortement testés, pixels en recette manuelle).

---

## 3. Constats de revue & statut

| Réf | Sévérité | Constat | Statut |
|-----|----------|---------|--------|
| — | INFO | Codex `discover_codex_rollout` : tolérance arrière 2 s (re-run < 2 s même cwd pourrait ré-attraper un rollout ; atténué par tie-break « plus récent »). | À confirmer en recette réelle. |
| — | INFO | Codex `rollout_cwd` lit le fichier entier (seule la 1ʳᵉ ligne sert). | Inefficacité mineure, pas un bug. |
| — | INFO | Codex shapes d'outils/gestes = liste défensive (MVP couvre la parole). | À confirmer sur session réelle avec commandes. |
| — | INFO | Couverture UI basse (PtyTerminal/NextStepPanel/App shell/MainCourante). | Par conception (recette manuelle). |

**Aucun défaut bloquant** (contrairement à V1 en v0.10, corrigé et tenu).

---

## 4. Invariants vérifiés

- **Façade unique** : `invoke/listen/fetch` hors `src/api/backend.ts` = 0.
- **CSP** stricte jamais null ; vignettes **et chartes** servies en `'self'` ; aucune origine nouvelle.
- **Secrets keychain** write-only ; `is_secret` robuste au namespace `project_team:` (fix V1 v0.10 tenu).
- **Cœur runner Claude L10 non régressé** : `pty_runner_open` claude byte-équivalent (scrub env
  `CLAUDE_CODE_*`, `validate_cwd`, allowlist, `session_id`, trust, transcript) ; `tail_resolved` =
  extraction verbatim de la boucle live-tail (partagée Claude/Codex).
- **Runner Codex** défensif : résolution binaire pure testée, découverte rollout robuste
  (canonicalisation cwd, mtime, non-paniquante), mapping anti-doublon prouvé sur fixture.
- **Socle L0** : pathguard anti-traversal, `paths`/`IAKAFRAME_ROOT` cross-OS (zéro Windows en dur).
- **Pas de god-component** ; runner abstrait (zéro `runnerKind="claude-code"` en dur dans une vue).

---

## 5. Reste en recette manuelle (`tauri dev`, non couvrable unitairement)

- **Session Codex réelle** : dialogue de trust natif, rendu TUI xterm, shapes geste/outil sur
  session avec commandes, découverte du rollout sur `~/.codex/sessions` réel, re-run même cwd →
  rollout frais.
- **Repeinture des 10 chartes** : couleurs/rayons exacts ; polices web non bundlées → fallback
  générique (typo approchée).
- **Doc geste L5 dans le Journal** : une délégation tracée par L5 apparaît dans la vue Journal via
  la main courante L4 (recette inter-lot, CouchDB réel).
- Édition/persistance teams + popup `TeamPicker` ; main courante L4 sur CouchDB réel.

---

## 6. Différés connus

Runners RÉELS par agent / orchestration multi-agent (seul le coordinateur spawne) · exécution
`ollama`/`litellm` (bannerisés) · allowlist/trust par agent (restent globaux, AR-3) · live-switch
de coordinateur · rendu xterm TUI lent au scroll · polices web non bundlées (chartes typo
approchée) · vignettes-par-charte / teams par défaut (todo) · idempotence partielle `_id` côté L5
(hors dépôt).

---

## 7. Verdict

**PASS** — version **cohérente, sûre et verte** (291 front + 217 Rust, 0 warning, couverture en
hausse à 81.8 %). Socle L0 intact (fix V1 préservé), non-régression du runner Claude **prouvée au
code** lors de l'ajout du 2ᵉ runner Codex, Codex défensif, refactos front L12/L13 propres, pont
chartes L14 complet, invariants durs tenus. **Candidate `v0.11.0` scellée.**
