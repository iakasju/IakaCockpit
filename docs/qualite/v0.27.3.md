# Doc qualité — v0.27.3 (2026-07-13)

> **Patch recette** : (1) nom de projet = répertoire directement sous le chapeau `/work`
> (fin de la fragmentation ; non-projets → bucket `.folder`) ; (2) en-tête de la Table sur
> 2 lignes (fin du chevauchement). Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate `5b5d4d4`** : PASS — `cargo fmt --check` clean, clippy 0, **cargo test 325/0**, front 675.
- **Gate `f283265`** : PASS — typecheck/lint OK, **vitest 675/675**, front seul, garde L10 confirmée.

## Contenu livré
### `5b5d4d4` — Projet = répertoire directement sous le chapeau (Rust `economy.rs`)
Règle Stéphane : « le répertoire directement sous /work est le nom du projet ».
- `project_of(cwd)` prend désormais le **premier segment sous la racine chapeau**
  (`paths::resolve_hat_root()`, cross-OS `IAKAFRAME_ROOT`, zéro constante Windows), plus le
  dernier segment. `<root>/IakaCockpit/src-tauri` → **IakaCockpit** (racine + sous-dossiers réunis).
- **Non-projet** (segment sous /work SANS dépôt git `.git`) → bucket **`.folder`** (les dossiers
  divers d'Odin regroupés en UNE entrée). Racine exacte `/work` → `work` (portefeuille → Odin).
  cwd hors chapeau → dernier segment (fallback inchangé).
- Pur/testable (`project_of_with(cwd, root, is_project)`, prédicat injecté) ; git-check **caché
  par segment** (une vérif FS par nom, appelé seulement sous la racine). **Classification 100 %
  lecture** (seul accès FS = `.exists()`) — aucun déplacement de fichier, aucune migration ;
  l'index se reconstruit au démarrage/`analytics_refresh`. Cohérence auto partout (index/
  economy/activity/cost/deleg/attrib/scope).

### `f283265` — En-tête de la Table sur 2 lignes (front `WorkingView.tsx` + `app.css`)
L'en-tête entassait tout sur une ligne (projet · coordinateur · runner · Chat/Shell · Prochaine
étape · Délégations · Arbre/Couloirs) → chevauchement. Mise en page seule :
- **Ligne 1 = identité** (`.convtitle`) ; **ligne 2 = contrôles** regroupés dans `.convctrls`
  (`flex-wrap` = filet petit écran). `.convhead` en colonne. Handlers/états inchangés,
  **garde L10** (PtyTerminal monté une fois, jamais démonté) intacte. Vérifié CDP clair/sombre,
  1000px et 560px, aucun chevauchement.

## Commits (scellés dans v0.27.3)
`5b5d4d4` (projet = répertoire sous le chapeau + `.folder`) · `f283265` (en-tête 2 lignes).

## Différés (Analytics, prochain lot)
- **V3 Comparaison** : A · avant/après constaté (2 périodes réelles) + B · constaté vs hypothèse
  (re-tarifage à volume constant, par modèle puis par agent — débloqué par l'attribution).
- Index **incrémental** (mtime) · arbre de délégations multi-niveaux.
