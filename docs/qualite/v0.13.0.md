# Doc qualité — v0.13.0

> Scellée le 2026-06-30. Candidate `v0.13.0`. Précédente : `v0.12.0`.

## Thème de la version

**Identité « Atelier / Étagère / Table » + fondation « main courante par hook » + widgets
de la Table dérivés du transcript.** Le cockpit passe d'un mock validé (direction A) à des
vues **alimentées par de la vraie donnée de session**, selon le principe **« instrumenter,
pas gratter »**.

## Lots livrés

- **L15 — Noms de personnages des teams** : les 11 teams du catalogue iakagraph affichent le
  nom du personnage (Beast, Professor X…) ; source = `teams.json` amont (champ `name`).
- **Identité Atelier / Étagère / Table** : relabel d'EXPÉRIENCE (rail, vues, geste
  prendre→poser→ranger, accroche « Atelier »), socle technique intact (`IAKAFRAME_ROOT`,
  `ViewId`, rôles). Note : `specs/design/identite-atelier-etagere-table.md`.
- **L18 — Main courante par hook (fondation)** : généralise le hook L5 en émetteur de main
  courante structurée → CouchDB (lu par L4). Schéma unifié, émetteur **par source**. Hook
  `~/.claude/plan-courante.mjs` (plan complet, prouvé end-to-end CouchDB).
- **Widgets de la Table** (dérivés transcript/main courante) :
  - **#1** Étagère : bandeau KPIs + tuiles 3 colonnes (responsive-conteneur) + colonne économie.
  - **#2** Attribution : gouttière d'avatars (variante C).
  - **#3** Plan vivant : checklist depuis la main courante (hook).
  - **#5a** Économie : sparkline **multi-ligne** (coordinateur/délégués) + totaux.
  - **#6** Mémoire : jauge contexte + **frontière de compaction**.
  - **#7** Effets fichiers : **heatmap** fichiers × tours.
- **L19 — Gantt prévisionnel** :
  - **#9a** Réalisé : timeline dérivée des snapshots de plan + alerte de retard.
  - **#9b** Prévisionnel : baseline (durée estimée `[~Xmin]`, option A) + dépassement.
  - **Obligation coordinateur** (live) : `--append-system-prompt` injecte l'obligation de
    planifier + estimer ; le hook capte → Gantt en live. Honnête : pas d'estimation → réalisé seul.
- **Démo** : `iaka-demo` enrichi → tous les widgets de la Table sont pertinents en démo
  (fallback au rendu, dev-gardé, jamais de fausse donnée en prod ni d'écrasement live).

## Qualité (gate)

- **Front** : 360 tests verts (vitest), `tsc --noEmit` OK, ESLint **0 warning**, build Vite OK.
- **Rust** : 221 tests verts, `cargo fmt --check` OK, `cargo clippy -D warnings` OK.
- `bash scripts/quality.sh` → **Qualité : OK**.

## Différés tracés

- **Live** : recette réelle du prévisionnel (le coordinateur produit-il les `[~Xmin]` ?),
  recette EN, session Codex visuelle.
- **Gantt** : flèches délégation / user↔agent.
- **#5b** : treemap de coût **cross-projet** (agrégation multi-transcripts) — Étagère KPI.
- **Journal** : widgets de session (filtre projets, arbre délégations, frise mémoire).
- Attribution économie **par agent nommé** (corrélation sidechain→agent).
