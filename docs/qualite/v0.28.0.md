# Doc qualité — v0.28.0 (2026-07-13)

> **V3 Comparaison réelle** : la dernière perspective Analytics en placeholder devient réelle.
> Les **4 perspectives** (Dashboard / Timeline / Comparaison / Par agent) portent désormais de
> la **vraie donnée**. Cadré+coordonné 🟠 Aragorn · impl. 🔴 Gimli · gate 🟢 Legolas.

## Verdict qualité
- **Gate** (`2dc2c3c` + `141a6dd`) : PASS — `cargo fmt --check` clean, clippy 0, **cargo test
  326/0**, **vitest 685/0** (75 fichiers). Garde d'honnêteté V3-B prouvée au code + tests.

## Contenu livré
### `2dc2c3c` — commande `pricing_table` (Rust + façade)
`pricing.rs::to_table()` + commande `pricing_table()` (table de prix courante, lecture seule) +
façade `pricingTable()`. Sert le re-tarifage d'hypothèse côté front (qui recalcule des **$**,
jamais des tokens).

### `141a6dd` — V3 Comparaison (front)
- **V3-A · avant/après CONSTATÉ (100 % réel)** : `useCompare(rangeA, rangeB, project)` appelle
  coût + attribution + délégations pour **A ET B** (via l'index précalculé, scopé projet) →
  `computeCompareStats` : deltas **tokens / coût $ / coût-par-M tokens / délégations / temps**
  (chips ↑/↓ %, ton), **tokens par agent A vs B** (fusion par nom, coordinateur inclus),
  **verdict honnête dérivé des chiffres** (aucun texte inventé ; `null` si pas de base).
- **V3-B · constaté vs HYPOTHÈSE** : `repriceHypothesis` re-tarife les **tokens OBSERVÉS**
  sous un modèle cible (override source→cible, menus alimentés par `pricingTable`) — **les
  tokens ne bougent pas** (ratio de taux, matching préfixe + `[1m]`). **Étiquette gravée
  « hypothèse · à volume constant » affichée inconditionnellement** ; modèle cible sans prix →
  coût `—`/null ; agent untariffé/`unavailable` exclu et signalé.

## Gardes tenues
Façade D7 (`pricingTable` via backend.ts, aucun invoke hors façade ; front ne lit aucun
transcript/outputFile), D8 (compo dans `useCompare`/`compareModel`, `PerspectiveCompare` pur),
CSP, i18n parité fr/en, `DEMO_DATA_ENABLED=false`, range/scope-aware, perf via index précalculé,
**zéro fausse donnée** (réel ou placeholder ; hypothèse jamais présentée comme mesure).

## État Analytics (L30) — les 4 perspectives réelles
V1 Dashboard · V2 Timeline · V3 Comparaison (A réel + B hypothèse) · V4 Par agent (délégués +
coordinateur, tokens/coût réels via `outputFile`). Coût $ réel par période/modèle/projet ;
attribution par agent nommé ; index précalculé (build 2 phases) ; projet = répertoire sous le
chapeau (+ `.folder`).

## Commits (scellés dans v0.28.0)
`2dc2c3c` (pricing_table) · `141a6dd` (V3 A+B).

## Différés (Analytics)
Index **incrémental** (mtime) au lieu du rebuild complet · arbre de délégations **multi-niveaux**
· hypothèse V3-B **par agent nommé** (override par agent — l'attribution le permet).
