# Doc qualité — v0.24.0 (2026-07-13)

> **L29 — Swimlanes d'agents** : le rendu HORIZONTAL COMPACT des délégations (variante B du mock
> d'hypothèses), retrouvé avec Stéphane après recherche. Front seul.
> Cadré + coordonné par 🟠 Aragorn ; implémenté par 🔴 Gimli ; gate 🟢 Legolas ; mock de Loki.

## Verdict qualité
- **Gate L29** : PASS — `typecheck`/`lint` OK, **vitest 600/600** (67 fichiers), **aucun `src-tauri/`
  touché** (Rust intact).
- Gardes vérifiées : **zéro fausse donnée** (pas de barre/flèche sans `ts` ; barre `running` ouverte
  jusqu'à « maintenant », aucune fin/estimation inventée), `doneTs` non régressif, présentationnel pur
  D8, parité i18n fr/en, non-régression L24→L28.

## Contenu livré (L29)
Rendu **horizontal** des délégations = **variante B « Swimlanes d'agents »** (couloir par agent, axe
temps, flèches de délégation) :
- **F1** — `useAgentTasks` : nouveau `AgentTask.doneTs?` (ts de clôture, au `running→done`), non
  destructif.
- **F2** — `AgentSwimlanes.tsx` (présentationnel pur, SVG, calque `ActivityTimeline`) : **un couloir
  par agent** (coordinateur + délégués dédup), **axe temps X** (min→max des ts), **barres** par tâche
  (`ts`→`doneTs`, ou ouvertes jusqu'à `now` si `running` ; ambre/vert), **flèches de délégation**
  coordinateur → délégué (MVP 1 niveau), **ascenseur horizontal**, placeholder si vide.
- **F3** — Travail : **toggle « Arbre / Couloirs »** commutant `DelegationTree` (L28, vertical) et
  `AgentSwimlanes` (L29, horizontal), mêmes données ; défaut **Couloirs**. `DelegationTree` et
  `GanttPanel` (`GANTT_ENABLED=false`) restent dans le code.

## Historique du besoin
Recherche « arbre horizontal compact » → retrouvé dans le mock d'hypothèses
`A/concepts/hypotheses/arbre.html` (variantes A mini-map / **B swimlanes** / C subway) + version
aboutie `A/concepts/app/travail.html`. Choix Stéphane = **B**. Un mock horizontal a aussi été produit
par Loki (`A/concepts/arbre-horizontal.html`).

## Commit (scellé dans v0.24.0)
`135c688` — feat(delegations): AgentSwimlanes (variante B) + doneTs + toggle Arbre/Couloirs (L29).

## Différés tracés
Imbrication multi-niveaux (flèches via parent réel `parentUuid`/`isSidechain`) · prévisionnel/baseline
(features Gantt L20, hors B) · swimlanes au Journal · recette visuelle SVG manuelle.
