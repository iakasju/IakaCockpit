{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities minimales de la fenetre principale (L0).",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
