# OBSOLÈTE — ce cadrage repose sur un cadre FAUX (rejeté par Stéphane le 2026-06-27)

> **Ne pas exécuter ce fichier.** Il mettait le **runner au centre**, réduisait la **team à un
> casting de vignettes**, gardait des **settings globaux** et **différait** le runner/modèle par
> agent — un cadre **rejeté** par Stéphane.
>
> **Instruction valide → [`L11-teams-agents-definition.md`](./L11-teams-agents-definition.md)** :
> la **team** est l'objet de premier rang ; chaque **agent = persona + runner + modèle + skills** ;
> la **définition** team/agents (Settings) est livrée **en entier** ; seule l'**exécution** des
> runners est staged (coordinateur claude-code).
>
> *Fichier conservé en tombstone (impossible à supprimer depuis le rôle cadrage) ; tout le contenu
> historique a été retiré pour éviter toute reprise accidentelle du cadre faux.*
